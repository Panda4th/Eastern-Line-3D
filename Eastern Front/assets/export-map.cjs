const fs=require('fs'),vm=require('vm');const ctx={};vm.createContext(ctx);vm.runInContext(fs.readFileSync('d3.min.js','utf8'),ctx);vm.runInContext(fs.readFileSync('topojson.min.js','utf8'),ctx);const d3=ctx.d3,topo=ctx.topojson;const data=JSON.parse(fs.readFileSync('land-50m.json'));const land=topo.feature(data,data.objects.land);const W=2048,sc=W/(40*Math.PI/180),merc=l=>Math.log(Math.tan(Math.PI/4+l*Math.PI/360)),H=Math.round(sc*(merc(61.7)-merc(42)));const proj=d3.geoMercator().scale(sc).translate([-sc*10*Math.PI/180,sc*merc(61.7)]).clipExtent([[0,0],[W,H]]);let rings=[],pts=[];const sink={moveTo(x,y){if(pts.length)rings.push(pts);pts=[[x,y]]},lineTo(x,y){pts.push([x,y])},closePath(){if(pts.length)rings.push(pts);pts=[]}};d3.geoPath(proj).context(sink)(land);if(pts.length)rings.push(pts);fs.writeFileSync('assets/map-rings.json',JSON.stringify({width:W,height:H,rings}));console.log(W,H,rings.length)
